version: 2
updates:
  # Rust dependencies (workspace root Cargo.lock).
  - package-ecosystem: cargo
    directory: "/"
    schedule:
      interval: weekly
  # Keep the SHA-pinned GitHub Actions fresh.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
